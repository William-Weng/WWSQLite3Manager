import XCTest
@testable import WWSQLite3Manager

final class WWSQLite3ManagerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WWSQLite3Manager().text, "Hello, World!")
    }
}
